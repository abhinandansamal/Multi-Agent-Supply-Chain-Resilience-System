[pytest]
# 1. Add current directory to Python path so 'src' imports work
pythonpath = .

# 2. Where to look for tests
testpaths = tests

# 3. Configure AsyncIO (since we use async agents)
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function